CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(getCurrentTime)
SET(getCurrentTime_SOURCES main.cpp)
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})
ADD_EXECUTABLE(getCurrentTime ${getCurrentTime_SOURCES})